# Site statique : aucun build, on publie le dossier tel quel.
[build]
  publish = "."

# Empêche la mise en cache agressive du HTML (les assets versionnés peuvent l'être).
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache"
